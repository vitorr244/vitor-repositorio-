# Rotina para calcular o enésiomo termo da sequência de Fibonacci.

# 
# Definição do enésimo termo.
#

n = float(input("Que termo deve ser calculado? "))

m = n - 2
t1 = 0
t2 = 1

while (m > 0):
    termo = t1 + t2 
    t1 = t2
    t2 = termo
    m = m - 1

print("O ternmo " + str(n) + " é " + str(termo))
